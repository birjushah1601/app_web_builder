# This is a config for E2B sandbox template.
# You can use template ID (uhq0yq8mzbx5vrs0r7hb) or template name (atlas-next-ts) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("atlas-next-ts") # Sync sandbox
# sandbox = await AsyncSandbox.create("atlas-next-ts") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('atlas-next-ts')

memory_mb = 2_048
ready_cmd = "curl -fsS http://localhost:3000 > /dev/null"
start_cmd = "cd /code && pnpm dev --hostname 0.0.0.0 --port 3000"
dockerfile = "Dockerfile"
template_name = "atlas-next-ts"
template_id = "uhq0yq8mzbx5vrs0r7hb"
