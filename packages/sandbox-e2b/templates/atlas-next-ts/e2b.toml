# This is a config for E2B sandbox template.
# v2 (2026-05) — ships Tailwind 3 + shadcn/ui (11 primitives) + lucide-react +
# framer-motion + atlas-* CSS-variable design tokens. Published as a NEW
# template rather than replacing v1, so projects can opt in via
# ATLAS_DEFAULT_SANDBOX_TEMPLATE=atlas-next-ts-v2.
#
# v1 (atlas-next-ts, ID uhq0yq8mzbx5vrs0r7hb) remains live for existing
# projects.

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("atlas-next-ts-v2")

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('atlas-next-ts-v2')

memory_mb = 2_048
ready_cmd = "curl -fsS http://localhost:3000 > /dev/null"
start_cmd = "cd /code && pnpm dev --hostname 0.0.0.0 --port 3000"
dockerfile = "Dockerfile"
template_name = "atlas-next-ts-v2"
template_id = "75mxlaomm6h7fasald1h"
