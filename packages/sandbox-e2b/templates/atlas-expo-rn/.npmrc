# Hoisted layout (flat node_modules) so Metro's default resolver finds
# transitive deps like react-native-css-interop/jsx-runtime. Without this,
# pnpm's symlinked .pnpm/<pkg>/node_modules/<pkg> layout breaks Metro's
# expectations for the NativeWind babel transform.
node-linker=hoisted
